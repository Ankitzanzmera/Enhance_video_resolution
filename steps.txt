Conda enviornment = conda create -p -venv/ python==3.9 -y

copy the low resolution videos into input_videos

pip install -r requirements.txt

run split_into_frames.py

git clone https://github.com/xinntao/Real-ESRGAN.git

download pretrained_model

cd Real-ESRGAN

pip install -r requirements.txt - dependecies of Real-ESRGAN

